name: access_37
channels:
    - conda-forge
dependencies:
    - python ==3.7
    - scipy >=0.11
    - numpy >=1.3
    - pandas >=0.23.4
    - requests >=2
    # testing, etc
    - codecov
    - pytest
    - pytest-mpl
    - pytest-cov
    - twine